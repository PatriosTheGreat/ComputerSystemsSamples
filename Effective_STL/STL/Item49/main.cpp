#include <iostream>
#include <list>
#include <algorithm>
#include <deque>
#include <set>
#include <vector>
#include <chrono>

using namespace std;

int main() {
    vector<int> v(10); //create a vector of size 10

    // string s(10); //attempt to create a string of size 10
}